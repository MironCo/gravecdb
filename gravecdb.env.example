# GravecDB Configuration Example
# Copy this file to .env and customize for your environment

# =============================================================================
# Option 1: Single DSN (recommended)
# =============================================================================
# Format: gravecdb://[username:password@][host][:port]/[datadir]?[options]

# Development (no auth, local data)
GRAVECDB_DSN=gravecdb:///data

# Production with auth
# GRAVECDB_DSN=gravecdb://admin:supersecretpassword@0.0.0.0:8080/data

# With Ollama embeddings
# GRAVECDB_DSN=gravecdb://admin:secret@:8080/data?embedder=ollama://localhost:11434/nomic-embed-text

# With OpenAI embeddings
# GRAVECDB_DSN=gravecdb://admin:secret@:8080/data?embedder=openai://

# In-memory only (no persistence)
# GRAVECDB_DSN=gravecdb://:memory:

# =============================================================================
# Option 2: Individual environment variables
# =============================================================================
# These override values in DSN or provide defaults

# Authentication
# GRAVECDB_USERNAME=admin
# GRAVECDB_PASSWORD=supersecretpassword

# Storage
# GRAVECDB_DATA_DIR=./data

# Server
# GRAVECDB_HOST=0.0.0.0
# GRAVECDB_PORT=8080

# Embeddings (for semantic search)
# EMBEDDER_URL=ollama://localhost:11434/nomic-embed-text
# EMBEDDER_URL=openai://

# OpenAI API key (required if using openai:// embedder)
# OPENAI_API_KEY=sk-...

# =============================================================================
# Embedder URL Examples
# =============================================================================
# Ollama (local):
#   ollama://localhost:11434/nomic-embed-text
#   ollama://localhost/mxbai-embed-large
#   ollama://                                  (defaults: localhost:11434, nomic-embed-text)
#
# OpenAI:
#   openai://                                  (uses OPENAI_API_KEY env var)
#   openai://sk-xxx@api.openai.com/text-embedding-3-large
